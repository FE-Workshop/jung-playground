/**
 * @param {string}
 * @returns {Array} - The query key
 */

export const queryKeys = {
	poketmons: () => ['poketmon'],
	// users: (userId) => ['users', userId],
	// products: (productId) => ['products', productId],
};
